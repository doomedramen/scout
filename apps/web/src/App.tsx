import { useEffect, useState } from "react";
import { Activity, CircleHelp, Download, KeyRound, LayoutList, Network, Radio, ScanSearch, ServerCog, Terminal } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { api, type Device } from "@/lib/api";
import { SetupView } from "@/views/setup";
import { SystemsView } from "@/views/systems";
import { DeviceView } from "@/views/device";
import { NetworkView } from "@/views/network";
import { UpdatesView } from "@/views/updates";
import { RecoveryView } from "@/views/recovery";
import { AccessView } from "@/views/access";
import { ScopesView } from "@/views/scopes";
import { EnrollmentView } from "@/views/enrollment";
import { ServicesView } from "@/views/services";

type Page = "Systems" | "Network" | "Updates" | "Access" | "Scopes" | "Enrollment" | "Services";
const pages = [{ name: "Systems" as const, icon: LayoutList }, { name: "Network" as const, icon: Network }, { name: "Scopes" as const, icon: ScanSearch }, { name: "Enrollment" as const, icon: ServerCog }, { name: "Access" as const, icon: KeyRound }, { name: "Services" as const, icon: Terminal }, { name: "Updates" as const, icon: Download }];

export default function App() {
  const [auth,setAuth]=useState<"loading"|"signedOut"|"signedIn">("loading");const [page,setPage]=useState<Page>("Systems");const [demo,setDemo]=useState(false);const [query,setQuery]=useState("");const [selected,setSelected]=useState<Device|null>(null);const [help,setHelp]=useState(false);const [status,setStatus]=useState<import("@/lib/api").Status|null>(null);const [statusError,setStatusError]=useState(false);
  useEffect(()=>{let cancelled=false;api.owner().then(()=>{if(!cancelled)setAuth("signedIn")}).catch(()=>{if(!cancelled)setAuth("signedOut")});api.status().then((value)=>{if(!cancelled)setStatus(value)}).catch(()=>{if(!cancelled)setStatusError(true)});return()=>{cancelled=true}},[]);
  if(auth!=="signedIn")return <><header className="topbar auth-topbar"><a className="brand" href="/" onClick={(event)=>event.preventDefault()}><Radio size={24}/><span>Scout</span></a><Badge variant="outline">{status?.mode??"Self-hosted"}</Badge></header><SetupView onSignedIn={()=>setAuth("signedIn")}/></>;
  function navigate(next:Page){setPage(next);setSelected(null);setQuery("")}
  return <div className="app-shell"><header className="topbar"><a href="#" className="brand" onClick={(event)=>{event.preventDefault();navigate("Systems")}}><Radio size={24}/><span>Scout</span></a><span className="workspace-name">Personal network</span><div className="topbar-actions"><Badge variant="outline">{status?.mode??"Self-hosted"}</Badge><Button variant="ghost" size="icon" aria-label="Setup information" aria-expanded={help} onClick={()=>setHelp(!help)}><CircleHelp size={18}/></Button><div className="avatar" title="Single-owner workspace">O</div></div></header><div className="toolbar"><nav aria-label="Main navigation">{pages.map(({name,icon:Icon})=><button key={name} className={page===name?"active":""} aria-current={page===name?"page":undefined} onClick={()=>navigate(name)}><Icon size={16}/>{name}</button>)}</nav><Button variant="outline" size="sm" onClick={()=>{setDemo(!demo);setSelected(null);setQuery("")}}>{demo?<Activity size={14}/>:<Radio size={14}/>} {demo?"Exit demo":"Explore demo"}</Button></div><main>{status?.recoveryMode&&<RecoveryView onChanged={()=>window.location.reload()}/>} {demo&&<div className="notice demo-notice"><Activity size={15}/><span>Demo workspace. Devices, metrics, and network relationships are illustrative and never enter operational storage.</span><Badge variant="outline">Sample data</Badge></div>}{help&&<section className="setup-panel"><div><Terminal size={21}/><h2>First Linux agent</h2></div><p>Use protected owner setup, then create a one-time invitation. The agent sends real observations over its authenticated outbound connection.</p><code>scout-agent --daemon --invitation-file /run/scout/invitation</code><Button variant="outline" size="sm" onClick={()=>setHelp(false)}>Close</Button></section>}{selected&&(page==="Systems"||page==="Network")?<DeviceView selected={selected} demo={demo} onBack={()=>setSelected(null)}/>:<><div className="page-heading"><div><h1>{page==="Systems"?"All systems":page==="Network"?"Network map":page==="Scopes"?"Scopes":page==="Enrollment"?"Enrollment":page==="Access"?"Access":page==="Services"?"Services":"Agent updates"}</h1><p>{page==="Systems"?"Every machine. One clear view.":page==="Network"?"Understand how infrastructure connects, with evidence.":page==="Scopes"?"Define where Scout may observe and enroll.":page==="Enrollment"?"Bounded discovery and automatic enrollment progress.":page==="Access"?"Resolve credentials and trust without exposing secrets.":page==="Services"?"Provider health and associated service entities.":"Keep agents current, including on isolated networks."}</p></div>{page==="Systems"&&<Button variant="outline" onClick={()=>setHelp(true)}><Terminal size={15}/>Agent setup</Button>}</div>{page==="Systems"&&<SystemsView demo={demo} query={query} onQuery={setQuery} onSelect={setSelected} onSetup={()=>setHelp(true)}/>} {page==="Network"&&<NetworkView demo={demo} onSelect={setSelected}/>} {page==="Scopes"&&<ScopesView/>} {page==="Enrollment"&&<EnrollmentView/>} {page==="Access"&&<AccessView/>} {page==="Services"&&<ServicesView/>} {page==="Updates"&&<UpdatesView/>}</>}</main><footer className="footer"><span>Self-hosted · single owner</span><div aria-live="polite">{statusError?<><span className="dot amber"/>API unavailable</>:<><span className={`dot ${status?.database==="connected"?"healthy":"muted"}`}/>{status?`API ${status.database}`:"Connecting to API…"}</>}</div></footer></div>;
}
