# Local development only. Generate a password; do not commit .env.
SCOUT_DB_PASSWORD=
# npm run setup generates both values consistently in a private .env.
# Unencrypted database transport is only for the loopback-bound local container.
SCOUT_DATABASE_URL=
